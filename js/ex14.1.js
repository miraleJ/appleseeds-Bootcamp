function getSum(arr1, arr2){
    let sum = 0;
    for (let i=0; i < arr1.length; i++){
    sum += arr1[i];
    }
    for (let i=0; i < arr2.length; i++){
    sum += arr2[i];
    }
    }
    getSum([1,2,3],[5,66,23]);

//row 3
//the calling to the function is not good (,)

    // row 4
    // the log of the terminal
    // sum is defined as const and cann't be reassigned